#include "main.h"

/**
*_isalpha - checks for alphabetic  chars.
*@c:the character being checked
*Return: return 1 if char is alphabetic  else return 0
*/
int _isalpha(int c)
{
if
((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
return (1);
else
return (0);
}
