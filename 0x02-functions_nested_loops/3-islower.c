#include "main.h"

/**
*_islower - checks for lower case characters. if
*@c:the character.
*Return: return 1 if char is lowercase else return 0
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}

