#include <stdio.h>
#include <stdio.h>
/**
 * main -print the program name
 * @argc: argument count
 * @argv: array of pointer to char
 * Return: zero
 */

int main(int argc, char *argv[])

{
		printf("%d\n", argc);
	}

return (0);
}
