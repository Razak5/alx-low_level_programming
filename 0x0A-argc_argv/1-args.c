#include <stdio.h>
#include <stdlib.h>
/**
 * main -print number of args passed to program
 * @argc: argument count
 * @argv: array of pointer to char
 * Return: zero
 */

int main(int argc, char *argv[])

{
		printf("%d\n", argc);
	}

return (0);
}
